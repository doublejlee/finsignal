# Minimal dependencies for the DEPLOYED serving layer (Streamlit dashboard).
# Streamlit Community Cloud installs from this root file. The dashboard only reads
# the committed DuckDB file, so it needs none of the ML/ingestion stack.
#
# For the full local pipeline (ingestion, NLP, backtest, API), use:
#   pip install -r requirements-pipeline.txt
streamlit==1.57.0
duckdb==1.5.3
pandas==3.0.3
