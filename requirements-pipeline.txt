# Full local stack: ingestion, NLP/ML, backtest, API, and dashboard.
# Install:  pip install -r requirements-pipeline.txt
# Then the spaCy model (not on PyPI):  python -m spacy download en_core_web_sm
#
# The DEPLOYED dashboard uses the lighter root requirements.txt instead — it reads
# the committed DuckDB file and needs none of the ML/ingestion stack below.

# API
fastapi==0.136.3
uvicorn==0.48.0

# Storage / data
duckdb==1.5.3
pandas==3.0.3

# NLP / ML
transformers==5.9.0
torch==2.12.0          # FinBERT inference backend
spacy==3.8.14          # sentence splitting; also needs: python -m spacy download en_core_web_sm
sentence-transformers==5.5.1   # RAG sentence embeddings (all-MiniLM-L6-v2)

# Dashboard
streamlit==1.57.0

# Ingestion
youtube-transcript-api==1.2.4
browser-cookie3==0.20.1
google-api-python-client==2.196.0
requests==2.34.2

# Backtesting
yfinance==1.4.0

# Reporting (README screening chart only — not needed for serving)
matplotlib==3.10.9

# Config
python-dotenv==1.2.2
